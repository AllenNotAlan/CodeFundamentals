using System;
using CodeFundamentals;

class Program
{
    static void Main(string[] args)
    {
        int[] arr = [1, 2, 3, 4];

        // ArraysSolution.ReturnProductArrayExceptSelf(arr);
        ArraysSolution.ReturnProductArr(arr);
    }
}
